# Copy to .env and replace with your own secrets-manager reference.
# Do NOT put the raw key here — this should resolve via `op run` (or your
# secrets manager's equivalent), never hold a real secret at rest.
# See README Setup section.
ANTHROPIC_API_KEY=op://<vault>/<item>/credential
